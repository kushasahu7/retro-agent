# NEVER commit anything derived from local sessions.
archive/
*.db
*.db-shm
*.db-wal
*.sqlite*
bundle/
bundle-*/
*.jsonl
*.jsonl.gz
__pycache__/
*.pyc
.DS_Store
.venv/

# local state: consent record, encryption salt
config.json
.archive.lock

# generated output at the repo root (real activity data); docs/ examples are synthetic
/*.svg
/*.md.bak
